<Window x:Class="STEP_JSON_Application_for_ASKON.JsonComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение JSON" Height="400" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="{Binding FileName1}" FontWeight="Bold" Margin="5" />
            <RichTextBox Name="File1RichTextBox"  VerticalScrollBarVisibility="Auto"  />
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="{Binding FileName2}" FontWeight="Bold" Margin="5" />
            <RichTextBox Name="File2RichTextBox"  VerticalScrollBarVisibility="Auto" />
        </StackPanel>
    </Grid>
</Window>